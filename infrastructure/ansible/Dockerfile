# Dockerfile for running Ansible with Kubernetes capabilities
FROM python:3.11-slim

# Install Ansible and the Kubernetes client library
RUN pip install ansible kubernetes

# Set up a working directory
WORKDIR /ansible
